Mini task queue experiment in Go + gRPC + k3s.

Producer -> Coordinator -> Workers communicating via gRPC RPCs.

Everything containerized with Docker and deployed to a single-node k3s cluster.

Learning project to explore building simple distributed systems.

Work in progress, as too many of my projects are.
